{{ text_field(__('Title'), 'title', {
        'value': data.title
    })
}}

{{ text_field(__('Banner'), 'banner', {
        'value': data.banner
    })
}}

{{ text_field(__('Link'), 'link', {
        'value': data.link
    }) }}
